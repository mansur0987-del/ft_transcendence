import { Ref } from "../../api";
export interface UseMediaDevicesState {
    deviceId: string;
    groupId: string;
    kind: string;
    label: string;
}
export declare function useMediaDevices(runOnMount?: boolean): {
    devicesState: Ref<UseMediaDevicesState[]>;
    isTracking: Ref<boolean>;
    isTracked: Ref<boolean>;
    start: () => void;
    stop: () => void;
};
