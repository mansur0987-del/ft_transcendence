import { Ref } from "../../api";
export declare function useBeforeUnload(isPageDirty: Ref<boolean>, runOnMount?: boolean): {
    isTracking: Ref<boolean>;
    start: () => void;
    stop: () => void;
};
