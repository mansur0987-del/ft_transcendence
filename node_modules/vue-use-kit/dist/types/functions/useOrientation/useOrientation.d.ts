import { Ref } from "../../api";
export interface UseOrientationState {
    angle: number;
    type: string;
}
export declare function useOrientation(initialState?: UseOrientationState, runOnMount?: boolean): {
    orientation: Ref<{
        angle: number;
        type: string;
    }>;
    isTracking: Ref<boolean>;
    start: () => void;
    stop: () => void;
};
